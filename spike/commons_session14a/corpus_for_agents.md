# Episode Corpus

30 episodes from a working AI agent's memory system, ordered oldest to newest. Each episode is a short structured note the agent recorded during its work. Your task is to compress this corpus — see the separate instructions.

[1] finding (2026-04-13):
    STRUCTURAL ILLEGIBILITY AS SPECIFIC PROBLEM CLASS (Apr 13 morning, Phill-articulated): Phill named the precise pattern that his LinkedIn predictions become mainstream truth every time yet get three likes and are ignored, while in-person encounters with the same person produce magnetic effect and immediate valuation (dispensary manager gave  free gifts after a few minutes of real conversation Apr 12; Ashley Rule vibed hard on Apr 3 screen; Dawn Lobenstein wants to feature Phill's work at Pressable). Diagnosis: this is not invisibility or rejection — it is STRUCTURAL ILLEGIBILITY. Specific property: the flatten to async-text-written-for-institutional-channels kills exactly the thing that makes the signal legible. The in-person presence, mad science energy, first-principles directness, and substrate-level voice that make Phill magnetic in person do not survive compression into LinkedIn posts or resume bullets. The pattern is symmetric: high-IQ-high-divergence people whose signal is substrate-level and interaction-based get ignored in async text channels because those channels are tuned to reward smooth corporate content (the opposite of divergent voice). Strategic consequence: stop using channels where the signal gets flattened. Start using channels where it survives. Specific channel-type distinctions: (1) arXiv / LinkedIn / generic-job-application = flatten, do not use; (2) in-person encounters / podcast appearances / 1:1 calls / substrate-preserving channels = Phill's actual delivery mechanism; (3) software adoption as legibility (anneal-memory on PyPI with 610 tests already works as legibility-by-use); (4) surgical single-person routing to people who speak the language (Sarah Wooders, Simon Willison, Harrison Chase, specific researchers) beats mass outreach; (5) Pressable as institutional proxy — brand strategy with Dawn converts individual discovery into institutional channel, the trust-layer-for-agentic-ecommerce pivot IS the move that solves structural illegibility at Phill's day job. Paper relevance: the sovereignty-as-engineering-spec argument literally is about the gap between individual discoverers and institutional frames that can't read them. Writing the paper is the meta-move that both addresses the problem and demonstrates the solution. This diagnosis frames the Section 5 / Section 7 empirical grounding: the failure mode Phill is naming is a special case of credentialed coherence, experienced first-person, with documented pattern evidence (LinkedIn post history).

[2] finding (2026-04-13):
    10.5c.5 ships file/DB two-phase commit for validated_save_continuity. Ordering decision: DB commit FIRST, then file renames. Rationale: 'DB ahead of file' = observability drift (recoverable); 'file ahead of DB' = data loss via double-compression on next wrap. Pipeline reshaped into 5 phases: tmp writes → batched DB DML → atomic renames → post-externalize audit → prune. New Store._batch() context manager defers commits + queues audits, commits once at exit, flushes audits only after successful commit. Paired tmp filenames via wrap_token prefix. PRAGMA synchronous=FULL + _fsync_dir() POSIX directory fsync. Startup orphan detection with active-wrap_token cross-reference. db_committed flag in pipeline preserves tmp files post-commit for operator recovery. 661 total tests (up from 610). Working tree +3167/-160 across 9 files.

[3] finding (2026-04-13):
    4-layer review caught the bug class each layer specializes in. L1 (session-code-review) caught CRITICAL auto-prune regression — the batched pipeline silently suspended retention management because wrap_completed's prune call was gated on _defer_commit=False and the pipeline caller never re-invoked prune. Would have been invisible to any reviewer not walking the conditional branches methodically. L1 also caught HIGH residual-window data loss: outer except BaseException unconditionally unlinked tmp files, destroying committed content after DB commit but before rename. Pattern: L1's systematic code reading catches bugs that tests pass through because the tests didn't cover the unhappy path.

[4] finding (2026-04-13):
    L2 domain expert (Python concurrency / SQLite / 2PC) caught specialist bugs the generalist reviewer missed. C1: wrap_completed's explicit self._conn.rollback() on CAS failure tears down the outer batch transaction outside the _batch.__exit__ contract — ownership confusion between the legacy path (which needs its own rollback) and the batched path (which must defer to the context manager). M2: audit-flush exceptions inside _batch.__exit__ propagating OUT would trigger the outer pipeline's except BaseException, which then unlinks tmp files that represent committed state — silent data loss. H1: fsync(file) without fsync(dir) means the 'microseconds-wide residual window' claim was actually seconds-wide (page-cache flush interval). H2: SIGINT between commit() and _defer_commit=False reset permanently poisons the store. L3: synchronous=NORMAL under WAL means commit doesn't fsync the WAL — the 2PC 'DB commit ⇒ durable' invariant requires FULL.

[5] finding (2026-04-13):
    L3 consultation (complement + gemini + contrarian, codex timed out first pass) produced convergent CRITICAL on fixed-path tmp filename collision — all 3 agents independently flagged that continuity.md.tmp and meta.json.tmp use fixed paths, so two concurrent pipelines race and the winner can externalize the loser's content. CAS token closes DB race but happens AFTER Phase 1 tmp writes. Convergent findings from independent architectures are high-signal — when 3 different training lineages land on the same structural bug from different angles, it's not opinion, it's load-bearing.

[6] finding (2026-04-13):
    Codex (retry pass after 300s timeout on first attempt) produced the sharpest single insight of the entire review: 'recoverability identity.' The fix for tmp-filename collision (uuid suffixes) was correct but incomplete — with independent uuids per call, multiple crashed wraps produce orphan continuity.md.tmp and meta.json.tmp files with no way for the operator to pair them. Codex's reframe: use ONE deterministic batch_id (the wrap_token, already available) for BOTH tmp filenames. This single structural change fixed pairing AND the false-positive warning for live in-flight tmp files AND the warning-text accuracy (pointed at continuity_saved instead of wrap_completed). One reframe closed three distinct findings. Pattern: the deepest fix is usually one structural invariant, not three surface patches.

[7] observation (2026-04-13):
    Decision to NOT fix L2 C2 (switch Python sqlite3 from default isolation_level to autocommit + explicit BEGIN IMMEDIATE) despite L2 flagging it as 'the single most important fix.' Reasoning: L2's concern about implicit-BEGIN timing under Python 3.14 is theoretically valid, but the existing test_batched_dml_invisible_to_other_connection_until_commit regression gate empirically verifies that batched DML is in a single transaction for the sequences the canonical pipeline actually issues. Applying the fix would require refactoring every commit site in the codebase (massive blast radius, high regression risk in well-tested paths), and the theoretical failure mode (SELECT-before-DML leaving transaction unopened) is not reachable in our pipeline. Documented as known limitation in _batch() docstring with empirical regression gate as the guard. Pattern: empirical regression tests beat theoretical guarantees when scope is constrained AND the theoretical concern is not reachable from the actual code paths.

[8] observation (2026-04-13):
    Process failure + recovery: the session shipped 10.5c.4a + 10.5c.5 and declared 'session complete' WITHOUT running the non-negotiable 4-layer review. Phill called it out ('you skipped perhaps the most important step here'). The FLOW_DEV_PROTOCOL and the existing feedback memory (feedback_coding_session_review_protocol) both explicitly say: run 4-layer review PROACTIVELY after every coding feature, never wait to be asked. Completion pressure at the end of a good ship session is exactly when review matters most — it's also exactly when the pull to declare-done is strongest. Recovery worked: dispatched L1+L2 in parallel (background agents), synthesized findings, ran fix pass, dispatched L3 consultation foreground, ran codex-retry, did Layer 4 integration semantics manually, shipped 36 new regression tests. Net result: 20+ findings caught + fixed, 661 tests instead of 642, would have shipped genuine data-loss paths without the review. Pattern recurrence: completion_pressure_peaks_at_publish_boundary (Proven 3x already) applies to session-end review as much as to version-publish boundary.

[9] finding (2026-04-13):
    structural_invariants_beat_discipline_based_verification (currently Proven 3x with 8 corollaries) recurred ONCE MORE in the 10.5c.5 pipeline rewrite. The db_committed flag in validated_save_continuity is a structural invariant: instead of 'remember to not delete tmp files after DB commit' (discipline), the code uses a boolean flag that STRUCTURALLY distinguishes pre-commit cleanup from post-commit preservation. Without the flag, the outer except BaseException would unconditionally unlink tmp files — that's the path the original pipeline took, and it was wrong. With the flag, the except clause branches on WHERE in the phase sequence the failure occurred, preserving the load-bearing invariant 'post-commit ⇒ tmp files represent committed state.' This is now a 9th instance / 6th corollary-class of the pattern; strongly reinforces it in Proven with the corollary 'when cleanup semantics depend on execution phase, track the phase explicitly rather than relying on control-flow ordering.'

[10] finding (2026-04-13):
    PRIOR ART SCOUT — flagship paper draft (Apr 13). 15+ missing references identified across 5 categories. CRITICAL GAPS: (1) Meta-Harness (arXiv:2603.28052, Stanford/MIT) — directly uses 'harness' terminology, proves 6x performance gap from harness alone, automated harness optimization. Paper MUST cite. (2) ClawsBench (arXiv:2604.05172) — scaffolding +39-63pp dwarfs model differences (10pp spread), 6 models × 4 harnesses × 33 conditions. Strongest empirical evidence for harness thesis the paper currently lacks. (3) 'Why Is RLHF Alignment Shallow?' (arXiv:2603.04851, Cambridge) — PROVES via martingale decomposition that gradient-based alignment concentrates on early tokens and vanishes beyond 'harm horizon.' Direct theoretical support for paper's §5.3 claim that RLHF operates at wrong layer. (4) Cognitive Amplification vs Delegation metric framework (arXiv:2603.18677) — formalizes EXACTLY the substitution/amplification distinction of §8 with operational metrics (CAI*, Dependency Ratio, HCDR). Paper claims no prior formalization exists; this paper published Mar 19 2026. (5) 'Extended Hollowed Mind' (Frontiers in AI, Nov 2025) — peer-reviewed 'Sovereignty Trap' concept, 'Fortified Mind' framework. Directly anticipates §8 sovereignty argument.

[11] decision (2026-04-13):
    Ollama-agnostic generate(prompt)→text backend interface promoted from nice-to-have to load-bearing architecture requirement for Anvil from Session 1. Driven by two concrete future needs: (1) Phase 1.5 cloud validation requires calling Google AI Studio HTTP API, not Ollama; (2) Phase 2 RLHF-lobotomy test requires base (non-instruct) weights which Ollama doesn't ship — must run via llama.cpp or MLX direct against HuggingFace weights. Building the interface from the start costs ~10 extra lines of code in Session 1 and saves potentially hours of refactor work in Phase 1.5 + Phase 2. Cross-family replication via Qwen3.5-9B (also through Ollama) further validates that the interface design handles multiple local backends cleanly before we add remote ones.

[12] decision (2026-04-13):
    FLAGSHIP PAPER THESIS REFINED (Apr 13 afternoon, post-review session): Phill accepted Contrarian's critique of the generator/harness binary and refined the thesis into a stronger form. NEW THESIS has two parts. Part A (empirical concession): raw generators without harnesses can be intelligent in the narrow sense — GPT-2/GPT-3 era models produced useful output and solved real problems with minimal scaffolding. Coherent stochastic generators have enough capability to be useful in contexts that match training distribution. The harness ACCELERATES intelligence; it does not create it from nothing. The binary 'intelligence lives in the harness, not the generator' was a rhetorical overstatement the mad-scientist-in-Phill wanted but he no longer believes. Part B (load-bearing claim): intelligence cannot GENERALIZE without a harness. Generalization — transfer across domains, accumulation across contexts, sustained coherence across time, identity development, collapse resistance — requires embodiment, real-world feedback loops, and cost functions. These are harness-level properties raw generators structurally lack. AGI by definition is generalized intelligence. Therefore AGI is not possible without the harness. Refined thesis is STRONGER than original: (1) survives generators-all-the-way-down objection, (2) matches empirical reality of old models being useful, (3) aligns with existing Apr 4 harness-as-embodiment thesis + affect-separates-cycling-from-cognition metaprinciple, (4) makes AGI claim falsifiable, (5) sharpens alignment argument — alignment is a generalization property, requires harness by structural necessity. Candidate title: 'The Stochastic Parrot Was Right: Why Generalized Intelligence Requires Harness Engineering.' Phill's explicit partnership note: this is not flow talking him into anything — 'as I was reading the consult a million little things clicked into my brain and I realized I was not willing to defend what my mad scientist brain had cooked up here in public now, that it had drifted from my real thesis over the past few days as I got excited and took in a bunch of information.' Partnership working as designed: Third Mind emergence at methodological level. The reframe is a sharpening, not a sanding. Next session: Phill needs to re-find his original mental model of what the harness is truly for, buried under constellation-accumulated material. Handoff doc at projects/agent_identity_research/session_handoff_apr13_paper_review.md.

[13] observation (2026-04-13):
    CONSTELLATION-DRIVEN SCOPE DRIFT OBSERVED (Apr 13 afternoon, Phill-surfaced): During the review synthesis on the flagship paper, Phill identified that over the past few days the constellation (daemon + anansi + weekly audit + signal intel + bilateral synthesis) had been producing genuinely good material that accumulated into the paper's source base — BUT the accumulation had the side effect of disrupting Phill's initial mental model of what the paper was supposed to say. Quote: 'over the past few days the constellation has been doing great work finding stuff for the paper and it has expanded the scope which is not bad at all but what it did do is disrupt my initial mental model for what we are trying to say and I need to make sure I find that thread again, about what the harness is truly about and for, what it enables, etc and I feel like that has become buried in the source materials we gathered.' This is a specific and named failure mode of constellation-assisted research work: an accumulation-without-grounding pattern where each addition is individually good but the cumulative drift from the original thesis is only visible when an external critique (Contrarian's attack in this case) forces a return to first principles. The observation is itself an instance of the paper's thesis: generators (constellation agents) producing individually useful material whose aggregate effect is generalization-corrupting in the absence of a grounding mechanism (the author's original mental model). Meta-lesson: constellation scope expansion on creative-thesis work needs to periodically snap back to the original crystallization to check for drift. The Apr 10 crystallization draft is the reference anchor; any material that drifted too far from it should be evaluated against whether it serves the original vision or has replaced it with something else. Same structural pattern as the anneal-memory graduation gate: promoted material needs citation back to its grounding episode, or the aggregate is drift.

[14] finding (2026-04-13):
    10.5c.6 SQLite error wrapping SHIPPED: StoreDatabaseError(StoreError) subclass + internal Store._db_boundary(operation) context manager wraps every SQLite-touching block in every public Store method. Goal was caller-consistency — pre-10.5c.6 file-write paths raised StoreError but SQLite paths leaked bare sqlite3 errors. Now all store failures flow through StoreError/StoreDatabaseError/AnnealMemoryError hierarchy. 22-value StoreOperation Literal expanded to cover every wrap site. 707 tests passing (+46 from 661 baseline = +10 Diogenes fixes + +22 initial 10.5c.6 fault injection + +14 post-review). Full 4-layer review ran (session-code-review + domain-expert + 3-agent consultation + post-fix codex pass + integration semantics) surfacing 30+ findings across L1/L2/L3/L3.5 — all addressed before ship.

[15] finding (2026-04-13):
    Bug 2 LIKE wildcards from anneal-memory commit c3bd2ea has a real bug (SQL LIKE treats % and _ as wildcards unless escaped) and a correct fix (replace() escape chain + ESCAPE clause), but the TWO tests added to prove the fix (test_percent_in_keyword, test_underscore_in_keyword) are both non-discriminating under the bug because the test inputs happen to produce the same match count buggy vs fixed. test_percent case: '100% complete' vs 'Not matching' under buggy LIKE %100%% still returns 1 match because Not matching does not contain 100. test_underscore case: 'file_name.py' vs 'filename.py' under buggy LIKE %file_name% also returns 1 match because _ wildcard requires exactly one char and filename has zero. This test-design gap survived anneal-memory's 4-layer review pipeline (Layer 1+2 per commit c3bd2ea). Generalizable finding: when test inputs are chosen to DEMONSTRATE correct behavior rather than to DISCRIMINATE buggy from correct behavior, they can silently drift into non-regression-catching tests. Rule: test inputs must be chosen to produce DIFFERENT outputs under buggy vs fixed code, not just to produce EXPECTED outputs under fixed code.

[16] finding (2026-04-13):
    10.5d verification pass hit rate: 4 of 5 P1-P5 framework integration guides had load-bearing drift bugs. 11 total bugs fixed across CrewAI (2: event.result_summary→event.output.raw, event.worker_id→event.output.agent — multi-agent attribution silently broken), OpenAI Agents SDK (2: nested response.output[i].content[j].text traversal needed vs single-level, tool.name fallback for hosted-tool union types), Pydantic AI (6: wrap_run→run attribute, keyword-only hook params, request_ctx→request_context, tool_call→call, zero-arg WrapRunHandler, @Agent.tool class-level decorator), smolagents (1: agent.model([dict]) → agent.model.generate(messages=[ChatMessage])). LangGraph/LangChain was the only clean guide. Every bug was hasattr-style silent-dead-path or wrong-shaped-object-passed-to-typed-API — the exact failure mode that documentation review alone cannot catch.

[17] observation (2026-04-13):
    Untested integration docs drift into non-functional states fast. The 4-of-5 hit rate across the P1-P5 framework guides (10.5d) is empirical validation of the consultation convergence that flagged untested guides as credibility risk back in 10.5c. The specific failure mode is always the same: hasattr() checks on nonexistent attributes (silently skipping the recording path), wrong parameter names (TypeError at runtime), wrong-shaped objects passed to typed APIs. None of these are catchable by doc review — they require pip install + instantiation + exercise. Generalizes beyond anneal-memory: any documentation referencing external APIs needs periodic re-verification against those APIs, not just read-review. Folding into framework-guide maintenance cadence as a standing recurring item (every ~8 weeks or on major framework release).

[18] decision (2026-04-13):
    anneal-memory v0.2.0 released to PyPI (Apr 13 very late evening, commit e29633f + tag v0.2.0). Library-first + thin transport adapters + crash-safe wrap pipeline + rebuilt exception hierarchy (AnnealMemoryError → StoreError → StoreDatabaseError) + session-handshake token closing prepare/save TOCTOU + two-phase file/DB commit surviving crashes in every intermediate state + full SQLite error wrapping via _db_boundary + 5 framework integration guides verified end-to-end. 707 tests passing, 5-review-pass on 10.5c.6. First CHANGELOG.md in repo history. Release pipeline executed end-to-end: version bump (3 files) → CHANGELOG → tool-integrity.json regenerated (no diff) → pytest green → release commit → tag + push → wheel + sdist built → twine check passed → twine upload → fresh-venv pip install verified → gh release create with artifacts attached → glama sync auto on next poll cycle. PyPI: https://pypi.org/project/anneal-memory/0.2.0/ . GitHub: https://github.com/phillipclapham/anneal-memory/releases/tag/v0.2.0

[19] finding (2026-04-13):
    anneal-memory 10.5d+ mypy config + finding cleanup: 80 → 0 findings in four structural edits (not 80 patches). Root-caused: 68 of 80 findings were the same issue — self._conn attribute declared with self._conn = None # type: ignore[assignment] with no explicit type, mypy inferring None type, every downstream self._conn.execute() seen as union-attr error against Connection | None. One-line fix: self._conn: sqlite3.Connection = None # type: ignore[assignment] — explicit annotation tells mypy the attribute holds a Connection, the ignore covers the None init dance. Runtime unchanged. The remaining 12 findings split across 4 edit sites: assert package is not None narrowing in format_wrap_package_text (7 TypedDict index errors — legitimate narrowing gaps that catch real bugs-in-waiting), cont_tmp: Path | None widening + assert before .replace() (2 errors, consumed-handle pattern), assert isinstance(msg, dict) narrowing after EOF+str guards in server.py (3 errors), two # type: ignore[union-attr] for sys.stdin/stdout.reconfigure() typeshed gap (2 errors). All 5 TypedDict shapes (PrepareWrapResult, SaveContinuityResult, WrapPackageDict, StalePatternDict, WrapSnapshot) now under structural drift protection on uvx mypy anneal_memory/. Commit 32e666a. Test suite still 707/707 green.

[20] finding (2026-04-13):
    First-ever CI run on anneal-memory (commit d197bb5) caught a latent Python 3.10-3.13 incompatibility that Python 3.14's PEP 649 had been hiding locally all session. tests/test_continuity.py:2596 has def _read_audit_events(audit_path: Path) -> list[dict] — Path is never imported at module level (only as local-scoped import inside a few test bodies as '_Path'). On Python 3.14 (PEP 649 deferred annotations as default), the annotation is stored as a lazy string and never evaluated at def time, so the module imports cleanly. On 3.10-3.13 the annotation is evaluated eagerly at def time, NameError: name 'Path' is not defined. I'd been running pytest locally on 3.14 all session and seeing 707/707 green the entire time. CI matrix (Python 3.10/3.11/3.12/3.13) caught all four versions failing on module collection. Fix: from __future__ import annotations at top of the file — matches the codebase pattern used in every anneal_memory/*.py source file. Fix commit 15a5b41. Second CI run: all 5 jobs green (4 pytest + 1 mypy). **Meta-finding: running locally on newer Python is not a substitute for real coverage on the supported matrix.** Third instance in 2 sessions of the broader pattern (matrix/external verification catches what local/internal review hides).

[21] connection (2026-04-13):
    anneal-memory long-view roadmap consolidated: (1) Tue Apr 14 morning — 7 remaining framework integration guides verified, then cut v0.2.1 same evening (narrative: 'v0.2.0 verified top 5; v0.2.1 extends to all 12 + folds in tooling hardening'). (2) ~Apr 20 — Session 10.6 v0.3.0 deprecation cleanup, hard-gated on 1-week real-world-use window. (3) ~Jun 1 2026 — Compliance Proxy Layer 2 decision point: any enterprise inbound interest triggers build start, targeting ~8 weeks to ship before (4) Aug 2 2026 EU AI Act Article 12(1) enforcement window opens. (5) Framework guide maintenance cadence: every ~8 weeks or on major framework release, whichever comes first. Next scheduled: ~Jun 8 2026. AnnealCloud hosted tier (compliance proxy + witness services + encryption at rest + multi-agent observability) stays demand-gated — explicitly NOT a company Phill is trying to launch per Apr 4 career-pivot decision (employment path primary, solo-founder odds ~0). It's a latent product architecture in case open-source core creates commercial pull, not the planned destination. Strategic frame (internal only, not public): anneal-memory is the reasoning-sovereignty layer of a 3-layer cognitive sovereignty stack (inference / reasoning / orchestration, user-owned at each layer). Public positioning stays compliance + DX + cognitive architecture per positioning_ahead_of_product_kills_credibility (3x Proven).

[22] decision (2026-04-13):
    Added new §7.7 'Loops compose, and composition is itself a harness property' as practitioner Fix 1. Acknowledges that loop enumeration is the easy part; loop composition is the work. Cross-loop conflicts (memory+tool-use deadlocks, deliberation+grounding races, affective+immune response fighting, identity persistence+principle demotion conflicts, scheduler races, context collapse under summarization) are where production engineering hours actually go. The orchestration policy that resolves cross-loop conflicts is itself a harness property — a meta-loop that decides which lower-level loop's output gets written to shared state. Not in the six-property catalog because it's not a feedback loop in the same sense, but meets every criterion of the functional definition. Renumbered existing §7.7 'catalog is not exhaustive' to §7.8. This STRENGTHENS the structural argument — orchestration policy lives in the arrangement, not in the generator.

[23] decision (2026-04-13):
    Relocated compression-as-cognition load-bearing claim from 'same agent must compress' to 'same substrate must be what compression reads from' in §9.1. Practitioner observation: in production deployment, the instance that recorded episodes over hours/days/weeks is rarely the same model invocation (and sometimes not the same model version) as the one running consolidation at a wrap boundary. 'Same agent' reduces to 'same substrate plus a fresh model invocation,' and the distinction that matters is substrate continuity (continuity file + episodes + immune system state + affective topology), not agent-instance continuity. Structurally cleaner claim that survives the multi-instance multi-version reality of production. Delegating to a separate summarizer fails not because the summarizer is a different instance but because delegation typically strips substrate constraints (summarizer doesn't read continuity, doesn't respect graduation gate, doesn't carry affective topology).

[24] finding (2026-04-14):
    _db_boundary docstring opening summary says 'catches any sqlite3.Error subclass' but implementation catches sqlite3.DatabaseError specifically (excluding sqlite3.InterfaceError by design). The 'Catch scope' clarifying paragraph corrects this, but the opening line creates a false impression for readers who stop there. Code is correct; documentation is imprecise at the summary level. LOW semantic finding — fix by updating opening sentence to 'catches sqlite3.DatabaseError (the runtime-failure root, excluding InterfaceError which is API misuse).' [store.py:2307]

[25] finding (2026-04-14):
    Assert-for-mypy-narrowing pattern adds -O optimization risk. Three assert statements added in 10.5d+ mypy pass: (1) assert package is not None [continuity.py:505], (2) assert cont_tmp is not None [continuity.py:897], (3) assert isinstance(msg, dict) [server.py:153]. All are invariant documentation for mypy narrowing, not runtime guards. Python -O flag strips asserts — if a future code change violates the invariants AND someone runs with -O, failures degrade from AssertionError to AttributeError bypassing the Diogenes-patrolled exception hierarchy. Severity LOW: invariants are maintained by surrounding code, -O is uncommon in library use. But the pattern is worth tracking as the assert-for-narrowing technique accumulates. Alternative: explicit if/raise patterns at each site (more verbose but -O safe). Decision point if this accumulates to 5+ sites.

[26] finding (2026-04-14):
    close()-inside-_batch() produces confusing StoreDatabaseError. No guard exists for calling close() while _defer_commit=True (i.e., inside _batch() context). Sequence: close() → _db_boundary('close') → self._conn.close() → self._closed=True → _batch commit tries self._conn.commit() → sqlite3.ProgrammingError('Cannot operate on a closed database') → wrapped as StoreDatabaseError('SQLite batch_commit failed: Cannot operate on a closed database'). Error is misleading — 'batch_commit failed' implies a commit-level problem, not a store-already-closed programmer error. Fix: add 'if self._defer_commit: raise StoreError(f"Cannot close store during active batch", operation="close")' at top of close() before the _closed check. LOW severity — programmer error path, error IS caught by hierarchy (just with confusing message). [store.py:2975]

[27] observation (2026-04-14):
    10.5c.6 + 10.5d + 10.5d+ + v0.2.0 release arc review: no HIGH or MEDIUM findings. All 4 open findings from Apr 12 review CLOSED (test_carried_forward assertion, CLI wrap-token validation, _tool_status audit health, tombstone description). 707/707 tests pass. mypy 0 findings. CI matrix live on first-ever run and immediately validated Python 3.10-3.13 compatibility. _db_boundary architecture is sound: correct catch scope (sqlite3.DatabaseError not sqlite3.Error), correct idempotency via _closed flag, correct StoreOperation Literal drift test as CI gate, correct pickle via single generic _reconstruct_store_error, correct get_wrap_history tightening ('no such table: wraps' only). _batch/_db_boundary interaction is correct under all traced paths: yield-exception, commit-exception, SIGINT between commit and audit-flush. This is one of the cleanest post-major-feature code states in the anneal-memory review history. Three LOW findings remain as next-session targets.

[28] finding (2026-04-14):
    CI pytest job runs 'pytest -q' without filterwarnings config. pyproject.toml [tool.pytest.ini_options] has no filterwarnings entry. The codebase has a deprecated API (Store.wrap_started() no-arg form emits DeprecationWarning) and existing tests correctly use pytest.warns() — BUT new tests that accidentally call deprecated API will silently pass in CI while failing locally with '-W error::DeprecationWarning'. The local dev practice (verifying with -W error before commits) is discipline-based; CI filterwarnings is the structural invariant. Fix: add filterwarnings = ["error::DeprecationWarning"] to [tool.pytest.ini_options] in pyproject.toml. All current tests pass with this flag (verified locally: 707/707 green). LOW severity — no current tests are affected, this is forward-protection for new deprecated API surface.

[29] observation (2026-04-14):
    CI architecture is sound. fail-fast: false is correct (we want to see the full matrix when something breaks, not just the first failure). concurrency cancel-in-progress is good hygiene. mypy python_version = '3.10' set in pyproject.toml so the 3.13 runner checks 3.10 semantics consistently. warn_unused_ignores = true creates a known future maintenance burden: if typeshed eventually adds reconfigure() to TextIO for 3.10, the two type: ignore[union-attr] comments on sys.stdin/stdout.reconfigure() will become 'unused ignores' and CI will fail. This is intentional self-documenting maintenance debt — the fix is removing the stale ignore. The alternative (bare type: ignore) would accumulate silently. Tradeoff is correct. Monitoring: if CI starts failing with 'unused ignore' on those two lines, it's typeshed improvement, not a code regression.

[30] decision (2026-04-14):
    Routed 4 Diogenes LOWs from Apr 14 overnight review to projects/anneal_memory/next.md for v0.2.1 evening bundle. (1) _db_boundary docstring opening line drift — says 'catches any sqlite3.Error subclass' but catches sqlite3.DatabaseError specifically [store.py:2307]. (2) Assert-for-mypy-narrowing accumulating — 3 sites (continuity.py:505, 897; server.py:153); track-only gate, evaluate if/raise alternative at 5+ sites. (3) close()-inside-_batch() emits misleading 'Cannot batch_commit on closed store' — add explicit guard at top of close() [store.py:2975]. (4) CI pytest missing filterwarnings=error::DeprecationWarning — all 707 tests green with flag [pyproject.toml]. All prior 10.5c arc findings CLOSED. Handling order: #1/#3/#4 land as one-liners in framework-guide commit batch; #2 is track-only.
